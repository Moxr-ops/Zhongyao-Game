using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestMedicineDisplay : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        MedicineDisplayManager.instance.InitializeMedicineDisplay("¼¦×Ó»Æ");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
