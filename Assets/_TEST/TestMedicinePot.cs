using UnityEngine;

public class TestMedicinePot : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        MedicinePotManager instance = MedicinePotManager.Instance;

        string[] NodToClick = { "01" };
        string[] Medicine = {"»ÆÁ¬" };

        //instance.CreateMedicinePot(NodToClick, Medicine, new Vector3(-1, 0, -4));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
